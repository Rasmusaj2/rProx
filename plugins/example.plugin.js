module.exports = {
    name: "example",
    version: "1.0.0",
    description: "An example plugin for the rProx proxy.",

    setup(api) {
        // Functionality called when the plugin is loaded
    }
}